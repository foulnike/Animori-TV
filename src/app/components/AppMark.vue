<script setup lang="ts">
// Знак приложения в рельсе. Это та же картинка, что стоит иконкой окна
// и ярлыка: src-tauri/icons/128x128.png. Другого знака у программы нет.
//
// Прежде знак был набран вектором прямо здесь: цветок из пяти лепестков
// с вырезом play, собранный на глаз из поворотов одного лепестка.
// Лепестки не сходились, и на тридцати четырёх пикселях это было видно.
// Вектор ради вектора здесь не нужен: править в знаке нечего, а расходиться
// с иконкой окна он вправе ещё меньше.
//
// Взят 128×128, а не icon.png на 512: в рельсе знак 34 пикселя, и даже
// при трёхкратном экране ста двадцати восьми хватает с запасом, а
// двухсот килобайт в сборке — нет. Пятнадцать килобайт больше порога
// встраивания, поэтому Vite положит файл в assets с хешем в имени,
// и окно скачает его один раз.
//
// Знак прозрачный: подложки нет, фон показывает тема. На тёмной теме
// лепесток (#0b0716) сливается с панелью, и форму держит одна
// фиолетовая обводка (#a486ff) — на светлой наоборот, виден тёмный
// лепесток, а обводка тонет. Это и есть «андерграунд» раскраски.
// Размер задаёт тот, кто ставит знак: в рельсе это .am-side__logo
// в AppShell.vue — 34×34 и скругление 12 пикселей. Здесь только то,
// без чего картинка ломала бы строку.
import markSrc from '../../../src-tauri/icons/128x128.png'
</script>

<template>
  <!-- alt пустой: рядом в рельсе стоит название программы текстом,
       и второй раз проговаривать его незачем.

       draggable="false": иначе знак в рельсе таскается мышью, как любая
       картинка в браузере — в окне приложения это выглядит поломкой. -->
  <img class="am-mark" :src="markSrc" alt="" draggable="false" />
</template>

<style scoped>
/* Ширины и высоты здесь нет сознательно: их задаёт тот, кто ставит
   знак, и свой размер перебивал бы его класс. */
.am-mark {
  display: block;
  flex: none;
  object-fit: contain;
}
</style>
