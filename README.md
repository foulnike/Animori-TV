<div align="center">

<img src="https://raw.githubusercontent.com/foulnike/AniMori-AniList-Toolkit/main/src-tauri/icons/128x128@2x.png" width="128" alt="AniMori">

# AniMori — приложение для AniList

**Настольный клиент [AniList](https://anilist.co) для Windows со своим интерфейсом: списки, поиск, страницы тайтлов и студий, свой плеер — по-русски, без браузера и менеджера скриптов.**

[![Версия](https://img.shields.io/badge/%D0%B2%D0%B5%D1%80%D1%81%D0%B8%D1%8F-3.0.0--dev-02A9FF?style=flat-square&labelColor=0B1622)](CHANGELOG.md)
[![Лицензия](https://img.shields.io/badge/%D0%BB%D0%B8%D1%86%D0%B5%D0%BD%D0%B7%D0%B8%D1%8F-MIT-02A9FF?style=flat-square&labelColor=0B1622)](LICENSE)

![TypeScript](https://img.shields.io/badge/TypeScript-3178C6?style=flat-square&logo=typescript&logoColor=white)
![Rust](https://img.shields.io/badge/Rust-CE422B?style=flat-square&logo=rust&logoColor=white)
![Vue 3](https://img.shields.io/badge/Vue%203-4FC08D?style=flat-square&logo=vuedotjs&logoColor=white)
![Tauri 2](https://img.shields.io/badge/Tauri%202-FFC131?style=flat-square&logo=tauri&logoColor=black)
![Vite](https://img.shields.io/badge/Vite-646CFF?style=flat-square&logo=vite&logoColor=white)
![Windows](https://img.shields.io/badge/Windows%2010%2F11-0078D4?style=flat-square&logo=windows&logoColor=white)

[Состояние](#состояние) · [Что уже есть](#что-уже-есть) · [Чего здесь нет](#чего-здесь-нет) · [Сборка](#сборка-из-исходников) · [Документация](#документация)

</div>

---

**AniMori** — отдельная программа, которая работает с AniList напрямую: сама запрашивает данные, сама рисует экраны и подтягивает русские названия и описания с Shikimori и anime365.

Прежний десктоп 2.x был устроен иначе — окно с сайтом `anilist.co`, в которое сборка вшивала тот же юзерскрипт. От этой схемы отказались: полный экран, модель фокуса и оформление внутри чужой страницы нам не принадлежали. Версия 3.0 — свой интерфейс от начала до конца.

AniMori — неофициальный проект и не связан с командой AniList.

> [!NOTE]
> **Где что лежит.** У проекта два продукта, и живут они в разных ветках:<br>
> — ветка **`app-3.0-dev`** (эта) — настольное приложение со своим интерфейсом, теги вида `app-3.0.0`;<br>
> — ветка **`script`** — юзерскрипт для Tampermonkey, теги вида `script-2.1.0`;<br>
> — ветка **`main`** — история монолита 2.x, когда скрипт и десктоп собирались из одной кодовой базы. Из неё же раздаётся словарь перевода для скрипта и работает сторож датасета.

## Состояние

Версия 3.0 в разработке, готовых сборок пока нет. Последние выпуски в разделе [Releases](https://github.com/foulnike/AniMori-AniList-Toolkit/releases) (`v2.0.2` и раньше) — прежняя гибридная версия: она снята с поддержки и из рабочих веток удалена, но установленные копии продолжают работать.

Переезд на 3.0 произойдёт сам. Идентификатор программы (`com.foulnike.animori`), ключ подписи и адрес манифеста обновлений остались теми же, поэтому первый же тег `app-3.0.0` сдвинет `releases/latest`, и апдейтер внутри старой оболочки предложит обновиться. Настройки и токен лежат в том же профиле и подхватываются новой версией. Подробности — в [`docs/REPO-LAYOUT.md`](docs/REPO-LAYOUT.md).

Скриншоты появятся к первому релизу: снимки из README 2.x показывали сайт с юзерскриптом и новый интерфейс не описывают.

## Что уже есть

В меню пять разделов — **Главная**, **Моё**, **История**, **Поиск**, **Настройки**. Остальные экраны открываются из них: **Тайтл** — из списков и поиска, **Студия** — чипами карточки, **Просмотр** — кнопкой «Смотреть».

- **Русский по умолчанию** — интерфейс написан по-русски; названия и описания подтягиваются с Shikimori или anime365 с указанием источника, а часть имён приезжает готовым датасетом и показывается без сети вовсе.
- **Списки без ожидания** — коллекция держится местным снимком: открывается сразу, работает без сети и не исчезает при отключении счёта.
- **Список остаётся вашим** — ввоз с AniList слиянием или заменой, выгрузка в XML формата MyAnimeList (его примут Шикимори, AniList, Kitsu) и копия в облако, сейчас Яндекс Диск. Правки на сервер не уезжают — [`docs/STORAGE-AND-SYNC.md`](docs/STORAGE-AND-SYNC.md).
- **Свой плеер** — отдельный экран на прямых ссылках, без чужого кадра и его рекламы. Источников два: Aniliberty и Kodik. Кадр уходит из окна двумя путями: маленьким окном поверх всех программ или трансляцией на телевизор — [`docs/ARCHITECTURE-VIDEO.md`](docs/ARCHITECTURE-VIDEO.md).
- **Страница тайтла** — описание и названия по-русски, персонажи и персонал с русскими именами, студии — отдельным экраном, кадры и трейлер — плиткой, музыка — опенинги и эндинги с AnimeThemes полосой внизу окна.
- **Календарь выхода** — на главной, под шапкой: что и когда выйдет у того, что вы смотрите.
- **История** — что смотрели, свежее вперёд; продолжить можно в одно нажатие.
- **Поиск по-русски** — по названиям тайтлов, а также по персонажам и персоналу.
- **Запросы мимо страницы** — к API обращается процесс программы, а не веб-часть: ограничения CORS её не касаются, заголовки под контролем.
- **Прокси** — оболочка применяет прокси из настроек к окну при запуске; панель в настройках показывает исход щупа и живой запрос через канал и предупреждает, что настройка требует перезапуска.
- **Вход в AniList** — в отдельном окне; токен ложится в оболочку и наружу не уходит.
- **Автообновление** — при запуске программа сверяется с последним релизом, проверяет цифровую подпись пакета, ставит обновление и перезапускается.
- **Оболочка** — внешние ссылки уходят в браузер по умолчанию, окно перезагружается кнопкой в шапке. Полный экран оболочка умеет, но кнопки для него в интерфейсе пока нет.

## Чего здесь нет

| Чего нет | Почему |
| :--- | :--- |
| Перевода интерфейса по словарю | у приложения свой интерфейс; `dictionary.json` остался юзерскрипту и раздаётся из ветки `main` |
| Отправки правок на AniList | сервер их из программы не принимает, и решено не пытаться: список односторонний, вывоз — файлом или копией в облако |
| Блокировщика рекламы | он имел смысл только внутри окна с чужим сайтом и уничтожен вместе с ним |
| Манги | снята из приложения; в юзерскрипте осталась |
| Юзерскрипта и режимов сборки | конфиг сборки теперь один и безусловный — [`docs/REPO-LAYOUT.md`](docs/REPO-LAYOUT.md) |

## Требования

Windows 10 или 11 и компонент **WebView2** — в Windows 11 и актуальной Windows 10 он уже установлен, иначе установщик предложит его загрузить.

## Сборка из исходников

Нужны Node.js и [окружение Rust и Tauri](https://tauri.app/start/prerequisites/).

```bash
npm install
npm run dev:app        # передний край в браузере
npm run tauri:dev      # окно приложения
npm run build:app      # передний край → dist/app
npm run tauri:build    # установщик → src-tauri/target/release/bundle/nsis/
npm run typecheck:all  # ядро отдельно, затем всё вместе
npm test               # проверки с подменой моста, оболочка не нужна
```

`npm run tauri:build` подписывает пакет обновления, поэтому ждёт переменные окружения `TAURI_SIGNING_PRIVATE_KEY` и `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`. Свою пару ключей делает `npm run tauri signer generate`.

Исходники разложены по слоям: `src/shared/` — ядро (API, данные, кэш, мост), `src/app/` — экраны, компоненты и роутер, `src-tauri/` — оболочка на Rust. Ядро про оболочку не знает ничего: `npm run typecheck:shared` проверяет это отдельной сборкой, где каталога `src/app` не существует, и падает на любой стрелке из ядра в надстройку.

## Выпуск

Выпуск делает тег вида `app-3.0.0`: прогон на `windows-latest` сверяет номер в теге с `package.json`, компилирует Rust, собирает установщик, подписывает обновление и кладёт рядом `latest.json`. Описание релиза берётся из верхнего раздела `CHANGELOG.md`.

## Документация

| Документ | Вопрос |
| :--- | :--- |
| [`docs/REPO-LAYOUT.md`](docs/REPO-LAYOUT.md) | что лежит в этой ветке, а что в остальных |
| [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) | как устроено приложение |
| [`docs/DECISIONS.md`](docs/DECISIONS.md) | почему устроено именно так |
| [`docs/ROADMAP.md`](docs/ROADMAP.md) | что дальше |

Полный список — [`docs/README.md`](docs/README.md).

## Источники данных

| Источник | Назначение |
| :--- | :--- |
| `graphql.anilist.co` | данные и списки AniList |
| `shikimori.rip`, `shikimori.io` | русские названия, описания, персонажи, франшизы (зеркала одного сервиса, опрашиваются по очереди) |
| `smotret-anime.online`, `anime365.ru` | тайтлы и описания (альтернативный источник anime365) |
| `anilibria.top`, `kodik-api.com` | прямые ссылки на видео для своего плеера |
| `graphql.animethemes.moe` | опенинги и эндинги |
| `cloud-api.yandex.net` | облачная копия списка, только по нажатию и только с вашим пропуском |
| `github.com` | манифест обновлений и подготовленные данные (`foulnike/animori-data`) |

Запросы идут из процесса программы. Ваши данные на сторонние серверы не отправляются: токен и настройки остаются на вашей машине, кэш — локальный. Облачная копия уходит только на ваш собственный диск и только тогда, когда вы нажали кнопку. Как собираются подготовленные данные — в [`docs/DATA-PIPELINE.md`](docs/DATA-PIPELINE.md).

## Лицензия

[MIT](LICENSE) © foulnike

Лицензия покрывает код проекта. Данные сторонних сервисов ею не покрываются: русские названия, описания и имена принадлежат Shikimori и anime365 — они показываются со ссылкой на источник и в репозитории не хранятся.

Датасет русских названий — единственный набор данных, который мы выкладываем сами: он собирается в [`foulnike/animori-data`](https://github.com/foulnike/animori-data) и выходит под [CC0-1.0](https://creativecommons.org/publicdomain/zero/1.0/) — то есть без всяких условий и без требования атрибуции. Ссылка на Шикимори в «О программе» остаётся вежливостью к источнику. Подробности — в [`docs/DATA-PIPELINE.md`](docs/DATA-PIPELINE.md).

Сторонние сервисы (AniList, Shikimori, anime365, Aniliberty, Kodik) принадлежат их владельцам и используются через их публичные API.
