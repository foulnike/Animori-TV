node_modules/
dist/
dist-ssr/
src-tauri/target/

# МОНОЛИТ — НЕ ФОРМАТИРОВАТЬ.
# Это работающая версия 1.9.1, опубликованная на GreasyFork (script 572948).
# Она остаётся эталоном и источником правды до конца Этапа 1: по ней сверяются
# номера строк при нарезке на модули, а переформатирование сбивает и нумерацию,
# и сравнение с main.
animori.user.js

README.md
CHANGELOG.md

# Данные, а не код: словарь тянется с raw.githubusercontent.com и должен
# оставаться байт-в-байт таким, какой его ждёт релизная версия.
dictionary.json

# Генерируется npm.
package-lock.json
