# Хранение

## Настройки

`core/settings.ts`. Читать `settings.x` в момент использования, а не копировать
при загрузке. До `await loadSettings()` в модуле лежат дефолты, а импорты
выполняются до `start()` в `app/main.ts`.

Логгер внутри модуля недоступен: `utils/logger` сам читает настройки, и импорт
дал бы цикл.

## Хранилище моста

`Bridge.storage`. Хранилище моста про транзакции не знает, поэтому записи
выстраиваются цепочкой — `core/store-chain.ts`: идущая запись держит следующую
за собой. Порядок один на всех писателей.

## Снимок

`core/snapshot.ts` — то из данных пользователя, что потерять нельзя. Склад
ответов сети (`db.ts`) в него не входит: он расходен, его терять не жалко.

Базы приложения нет: источник правды — массив в памяти, на диске одна запись.
Плюс дубль файлом через `files.rs` на случай, когда хранилище окна почистили со
стороны. Имя файла — только из списка в `files.rs`, каталог только приватный.

## Склад

`core/db.ts`, IndexedDB: карточки (`mediaCache`), соответствия MAL, франшизы.
Инстанс базы приватен; через мост прятать не нужно — в WebView IndexedDB
работает напрямую.

Здесь же лежат ответы о доступности видео. Сроки — `core/cache-life.ts`.

## Датасет названий

Файлы выпуска на диске, читаются без сети: `core/dataset-names.ts` между
памятью и складом. Подробности обновления — в `DATA.md`.

## Облако

Яндекс Диск. Копия лежит в папке приложения (`app:`, область
`disk.app_folder`): корня Диска пропуск не видит. Пропуск приходит аргументом и
в приложении не хранится; по опубликованной ссылке копию только читают.

Облако — хранилище, а не второй хозяин: пишет и читает только по действию
человека, чужую правку не затирает молча. Формат копии — `core/cloud-file.ts`,
модуль чистый нарочно: облако единственное место, где список покидает машину,
и цена ошибки — чужие записи. Провайдер один.

## Выгрузка списка

`core/mal-xml.ts` собирает XML экспорта MyAnimeList — единственный формат
обмена, который принимают чужие сервисы. Ни сети, ни диска, ни моста: на входе
записи, на выходе строка, сохранение — забота экрана через `export.rs`.

Формат ключуется номером MAL, а список живёт на AniList: записи без номера
возвращаются поимённо, чтобы человек мог их донести руками.

## Список односторонний

Правки живут только в памяти коллекции и на сервер не уезжают. Перенос по
умолчанию сливает, а не замещает. Источников переноса два — AniList по входу и
Шикимори по нику, записи одного вида.
