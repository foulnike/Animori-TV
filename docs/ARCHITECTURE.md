# Устройство

## Слои

| Каталог       | Роль                                                                 |
| ------------- | -------------------------------------------------------------------- |
| `src/shared/` | ядро: данные и правила без DOM. Об интерфейсе и об оболочке не знает |
| `src/app/`    | надстройка: экраны, компоненты, роутер, темы                         |
| `src-tauri/`  | оболочка на Rust: окно, команды, файлы, прокси канала окна           |

Границу держит `npm run typecheck:shared`: в этой проверке каталога `src/app`
не существует, поэтому любая стрелка из ядра в надстройку ломает сборку.

## Ядро

Данные и правила. Сети здесь нет — она в `api/`, и зависимости идут оттуда
сюда.

| Модуль                                   | Что делает                                                             |
| ---------------------------------------- | ---------------------------------------------------------------------- |
| `core/collection.ts`                     | хозяин списка: единственный источник правды, записи в памяти           |
| `core/collection-view.ts`                | отборы и сортировки по списку, только чтение                           |
| `core/snapshot.ts`                       | снимок: то из данных пользователя, что терять нельзя                   |
| `core/store-chain.ts`                    | порядок записей в хранилище моста: вторая встаёт за первой             |
| `core/settings.ts`                       | пользовательские настройки; читать `settings.x` в момент использования |
| `core/db.ts`                             | склад ответов в IndexedDB: карточки, соответствия MAL, франшизы        |
| `core/cache-life.ts`                     | сроки хранения; разброс ±20 % считается от ключа, а не случайно        |
| `core/dataset-names.ts`                  | датасет названий на диске, читается без сети                           |
| `core/media-title.ts`                    | русские названия и описания: память, датасет, склад, сеть              |
| `core/media-parts.ts`                    | записи каталога одного номера MAL: порядок и представитель             |
| `core/media-looks.ts`                    | облик тайтла для сетки постеров                                        |
| `core/media-search.ts`                   | поиск: свой список и каталог, латиница и кириллица                     |
| `core/franchise.ts`                      | дерево франшизы по хронологии Шикимори                                 |
| `core/person-title.ts`, `person-link.ts` | русские имена людей и ссылка на человека из описания                   |
| `core/ratings.ts`, `studio-logos.ts`     | оценки площадок и литографии студий                                    |
| `core/rich-text.ts`                      | разбор описаний: BBcode Шикимори и полумаркдаун AniList                |
| `core/recs.ts`                           | рекомендации главной и профиль вкуса                                   |
| `core/playable.ts`                       | метка доступности видео: очередь на видимое, склад ответов             |
| `core/video.ts`                          | общий слой источников видео: форма и реестр                            |
| `core/mal-xml.ts`                        | выгрузка списка в XML экспорта MyAnimeList                             |
| `core/cloud.ts`, `cloud-file.ts`         | облачная копия списка и её формат                                      |
| `core/adult.ts`                          | единая точка отбора 18+                                                |
| `core/net-health.ts`                     | учёт доступности источников                                            |
| `core/proxy.ts`, `proxy-settings.ts`     | настройка прокси канала окна                                           |
| `core/types.ts`                          | формы данных AniList и Шикимори                                        |
| `utils/`                                 | журнал, DOM-мелочи, сличение имён                                      |

## Клиенты

`src/shared/api/` — сеть и разбор чужих ответов. Стратегия (что и когда
спрашивать) живёт в `core/`.

| Клиент                      | Источник                                                   |
| --------------------------- | ---------------------------------------------------------- |
| `anilist.ts` и спутники     | AniList GraphQL: каталог, списки, тайтлы, люди, расписание |
| `shikimori*.ts`             | Шикимори: карточки, поиск, люди, списки, журнал            |
| `anime365.ts`               | русские названия и описания по номеру MAL                  |
| `kodik.ts`, `aniliberty.ts` | видео                                                      |
| `animethemes.ts`            | опенинги и эндинги                                         |
| `yandex-disk.ts`            | облачная копия списка                                      |
| `dataset.ts`                | загрузка датасета названий                                 |
| `rate-limit.ts`             | общий ограничитель темпа: один на источник                 |
| `proxy-check.ts`            | живая проверка прокси запросом через мост                  |

## Мост

`IBridge.ts` — контракт среды: **что** умеет платформа, но не **как**.
Подсистемы: `storage`, `files`, `exportFile`, `http`, `anilist`, `clipboard`,
`shell`, `proxyDiagnostics`.

Реализация одна — `TauriBridge.ts`. Её подставляет сборка псевдопутём
`@bridge-impl`: `TauriBridge` создаёт `LazyStore` на верхнем уровне модуля, и
любой импорт файла даёт побочный эффект. Псевдопуть прописан в трёх файлах —
`vite.config.ts`, `tsconfig.json`, `tsconfig.shared.json`; при расхождении
сборка идёт, типы падают.

Из кода вне `src/bridge` импортируют только `@/bridge`.

## Экраны

Хеш-роутер (`app/router/`): страница живёт файлом внутри приложения. Переход
бывает записью в историю и заменой — без замены карточка и плеер образуют
кольцо «назад».

Девять экранов: `home`, `lists`, `history`, `search`, `media`, `studio`,
`player`, `settings`, `log`. В меню пять: карточка, студия, плеер и журнал
открываются из списков, поиска, карточки и адресом `#/log`.

`SCREEN_DEPTH` задаёт направление смены экрана: вкладки вровень, карточка
глубже вкладок, студия и плеер глубже карточки.

Новый экран — три места: имя в `routes.ts`, подпись в `SCREEN_TITLES`,
компонент в `App.vue`; меню — четвёртое и необязательное.

## Оболочка

| Команда                                      | Что делает                                   |
| -------------------------------------------- | -------------------------------------------- |
| `animori_reload`, `animori_restart`          | перезагрузить окно, перезапустить приложение |
| `animori_open_external`                      | отдать адрес системе                         |
| `animori_auth_start/_submit/_status/_logout` | вход в AniList                               |
| `animori_anilist_query`                      | запрос к AniList из процесса оболочки        |
| `animori_file_read/_write`                   | чтение и запись своих файлов                 |
| `animori_export_pick_dir/_write`             | выгрузка списка файлом                       |
| `animori_track_pick_dir/_write`              | выгрузка трека темы                          |
| `animori_proxy_status/_probe`                | состояние и проба прокси канала окна         |

Права — один файл `src-tauri/capabilities/default.json`. Список адресов в нём
окончателен: промах плагин отклоняет до сети.

Токен AniList не покидает Rust: команда принимает тело запроса, заголовок
авторизации подставляет оболочка. Вход — в браузере.

## Обновление

`app/update.ts` — сверка версии с выпусками GitHub и окно предложения. Своего
апдейтера у Tauri на Android нет (`tauri-plugin-updater` только для десктопа):
приложение читает список выпусков, берёт первый с приставкой `tv-`, выбирает
файл по разрядности и отдаёт адрес оболочке.

Мост `AnimoriUpdate` (`MainActivity.kt`) — два метода: `abi()` и `install(url)`.
Второй возвращает `false`, когда нет права устанавливать пакеты, и уводит в
настройки Android. Целостность проверяет система: установка встаёт только на
файл, подписанный тем же ключом.

Проверка идёт при старте, только на `am-lite`: на компьютере моста нет. Имена
файлов выпуска окончательны: `AniMori_<версия>_<armv7|arm64>.apk`.

## Признак слабой платформы

`app/platform.ts` ставит класс `am-lite` на `<html>` до `createApp()`. Признак
один — Android.

Из него же `canOpenOutside()`: на приставке браузера нет. Что глушится — в
`theme.css` у `.am-lite`, см. `INTERFACE.md`.
